package edu.arizona.kra.irb.pdf.sql.enums;

public enum Column {
    PROTOCOL_NUMBER,
    DESCRIPTION,
    FILE_ID,
    FILE_NAME,
    UPDATE_TIMESTAMP,
    PA_PROTOCOL_ID,
    SUBMISSION_ID,
    SUBMISSION_DATE,
    PAT_DESC,
    id,
    dest_type,
    protocol_number,
    huron_destination,
    dest_att_is_set,
    ui_filename,
    sftp_path,
    category,
    version_id,
    is_processed,
    error,
    document_id
}
